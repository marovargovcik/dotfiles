# No service manager in the user session: pipewire (exec'd from sway) spawns
# wireplumber and pipewire-pulse itself. Only *.conf files in this dir are loaded.
context.exec = [
    { path = "wireplumber" args = "" }
    { path = "pipewire" args = [ "-c" "pipewire-pulse.conf" ] }
]
