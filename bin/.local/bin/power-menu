#!/bin/sh
choice=$(printf 'lock\nsleep\nrestart\npower-off' | fuzzel -d --no-exit-on-keyboard-focus-loss)
case "$choice" in
    lock)      swaylock -f -c 000000 ;;
    sleep)     loginctl suspend-then-hibernate ;;
    restart)   loginctl reboot ;;
    power-off) loginctl poweroff ;;
esac
